import {Meta, Story, Preview} from '@storybook/addon-docs/blocks';
import {action} from '@storybook/addon-actions';
import Button from 'component/button';
import ButtonPrimary from 'component/button/primary';
import ButtonSecondary from 'component/button/secondary';
import ButtonTertiary from 'component/button/tertiary';
import ButtonDanger from 'component/button/danger';
import ButtonDangerSecondary from 'component/button/dangersecondary';
import ButtonSmall from 'component/button/small';

<Meta title="Button" component={Button} />

# Button

`Button` is used to create buttons.

<Preview>
  <Story name="button">
    <Button onClick={action('button-click')}>Click me</Button>
  </Story>
</Preview>

`Button` may span the full width of its parent.

<Preview>
  <Story name="button fullwidth">
    <div>
      <Button fullWidth>Click me</Button>
    </div>
  </Story>
</Preview>

`Button` may have a fixed width.

<Preview>
  <Story name="button fixedwidth">
    <div>
      <Button fixedWidth>Click</Button>
      <Button fixedWidth>me</Button>
    </div>
  </Story>
</Preview>

Use the variant `button/primary` for primary actions.

<Preview>
  <Story name="button primary">
    <ButtonPrimary>Click me</ButtonPrimary>
  </Story>
</Preview>

Use the variant `button/secondary` for secondary actions.

<Preview>
  <Story name="button secondary">
    <ButtonSecondary>Click me</ButtonSecondary>
  </Story>
</Preview>

Use the variant `button/tertiary` for tertiary actions.

<Preview>
  <Story name="button tertiary">
    <ButtonTertiary>Click me</ButtonTertiary>
  </Story>
</Preview>

Use the variant `button/danger` for actions that the user should be cautious
towards.

<Preview>
  <Story name="button danger">
    <ButtonDanger>Click me</ButtonDanger>
  </Story>
</Preview>

Use the variant `button/dangersecondary` for actions that the user should be
cautious towards.

<Preview>
  <Story name="button danger-seconary">
    <ButtonDangerSecondary>Click me</ButtonDangerSecondary>
  </Story>
</Preview>

Use the variant `button/small` for a less intrusive button.

<Preview>
  <Story name="button small">
    <ButtonSmall>Click me</ButtonSmall>
  </Story>
</Preview>

A `Button` may be disabled.

<Preview>
  <Story name="button disabled">
    <div>
      <Button disabled onClick={action('button-click')}>Click me</Button>
      <ButtonPrimary disabled>Click me</ButtonPrimary>
      <ButtonSecondary disabled>Click me</ButtonSecondary>
      <ButtonTertiary disabled>Click me</ButtonTertiary>
      <ButtonDanger disabled>Click me</ButtonDanger>
      <ButtonDangerSecondary disabled>Click me</ButtonDangerSecondary>
      <ButtonSmall disabled>Click me</ButtonSmall>
    </div>
  </Story>
</Preview>
