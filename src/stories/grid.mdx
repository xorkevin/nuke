import {Meta, Story, Preview} from '@storybook/addon-docs/blocks';
import {Fragment} from 'react';
import {Grid, Column} from 'component/grid';

<Meta title="Grid" component={Grid} />

# Grid

`Grid` displays a grid of elements. The grid is 24 units wide.

export const boxes = (
  <Fragment>
    <Column md={8} sm={12}><div style={{height: '256px', border: '1px solid red'}}></div></Column>
    <Column md={8} sm={12}><div style={{height: '128px', border: '1px solid red'}}></div></Column>
    <Column md={8} sm={12}><div style={{height: '256px', border: '1px solid red'}}></div></Column>
    <Column md={8} sm={12}><div style={{height: '128px', border: '1px solid red'}}></div></Column>
    <Column md={8} sm={12}><div style={{height: '256px', border: '1px solid red'}}></div></Column>
    <Column md={8} sm={12}><div style={{height: '128px', border: '1px solid red'}}></div></Column>
    <Column md={8} sm={12}><div style={{height: '128px', border: '1px solid red'}}></div></Column>
  </Fragment>
);

<Preview>
  <Story name="grid">
    <Grid>
      {boxes}
    </Grid>
  </Story>
</Preview>

Using the `strict` prop negates padding.

<Preview>
  <Story name="grid strict">
    <Grid strict>
      {boxes}
    </Grid>
  </Story>
</Preview>

The `nowrap` prop prevents wrapping.

<Preview>
  <Story name="grid nowrap">
    <Grid nowrap>
      {boxes}
    </Grid>
  </Story>
</Preview>

The `direction` prop may be used to determine in what direction items are displayed.

<Preview>
  <Story name="grid direction">
    <Grid direction="column-reverse">
      {boxes}
    </Grid>
  </Story>
</Preview>

The `justify` prop may be used to manage remanining space on the major axis.

<Preview>
  <Story name="grid justify">
    <Grid justify="center">
      {boxes}
    </Grid>
  </Story>
</Preview>

The `cjustify` prop may be used to manage remanining space on the minor axis.

<Preview>
  <Story name="grid cjustify">
    <Grid cjustify="center">
      {boxes}
    </Grid>
  </Story>
</Preview>

The `align` prop may be used to align items on the minor axis.

<Preview>
  <Story name="grid align">
    <Grid align="center">
      {boxes}
    </Grid>
  </Story>
</Preview>
